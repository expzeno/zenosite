## Mid-Task
Task: Build-to-live workflow for Lion to manually deploy to production
Next step: None — build-live.sh works, tar.gz generated. Lion can run `bash build-live.sh` anytime.
Context: Dev site stays noindex. build-live.sh transforms to index/follow, opens robots.txt, excludes dev files, outputs tar.gz for manual upload to production server. Nginx on prod needs `try_files $uri $uri.html $uri/ =404`.
